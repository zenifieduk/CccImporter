---
layout: layouts/base.njk
pagination:
  data: collections.tagList
  size: 1
  alias: tag
permalink: /blog/category/{{ tag | slug }}/
eleventyExcludeFromCollections: true
eleventyComputed:
  title: "Posts tagged with '{{ tag }}'"
  description: "All blog posts tagged with '{{ tag }}' on ClassicCarClubs.uk"
---

<link rel="stylesheet" href="/assets/css/blog.css">

<section class="hero-section secondary-hero-section" style="height: 30vh; min-height: 450px; background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('/assets/images/blog-header.avif'); background-size: cover; background-position: center;">
  <div class="hero-overlay"></div>
  <div class="container">
    <div class="breadcrumbs-container">
      <ul class="breadcrumbs">
        <li><a href="/" style="color: white;">Home</a></li>
        <li><a href="/blog/" style="color: white;">Blog</a></li>
        <li><a href="/blog/category/{{ tag | slug }}/" style="color: white;">{{ tag }}</a></li>
      </ul>
    </div>

    <div class="hero-content">
      <h1 class="hero-title">Posts tagged with "{{ tag }}"</h1>
      <p class="hero-subtitle">Browse all articles related to {{ tag }}</p>
    </div>
  </div>
</section>

<section class="blog-list">
  <div class="container">
    <div class="blog-sidebar-layout">
      <div class="blog-main">
        <h2>Articles tagged with "{{ tag }}"</h2>
        
        <div class="blog-grid">
          {% set taglist = collections.blog | getTaggedItems(tag) %}
          {% for post in taglist | reverse %}
            <div class="blog-card">
              {% if post.data.featuredImage %}
                <div class="blog-card-image">
                  <img src="{{ post.data.featuredImage }}" alt="{{ post.data.title }}">
                </div>
              {% endif %}
              <div class="blog-card-content">
                <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
                <div class="blog-card-meta">
                  <span class="blog-date">{{ post.date | blogDate }}</span>
                  <span class="blog-reading-time">{{ post.templateContent | readingTime }} min read</span>
                </div>
                <p>{{ post.data.excerpt or post.templateContent | excerpt }}</p>
                <a href="{{ post.url }}" class="read-more">Read More</a>
              </div>
            </div>
          {% endfor %}
        </div>

        <p class="back-link"><a href="/blog/">← Back to all articles</a></p>
      </div>

      <div class="blog-sidebar">
        <div class="sidebar-section">
          <h3>Categories</h3>
          <ul class="category-list">
            {% for tagName in collections.tagList %}
              <li><a href="/blog/category/{{ tagName | slug }}/" {% if tagName === tag %}class="active"{% endif %}>{{ tagName }}</a></li>
            {% endfor %}
          </ul>
        </div>

        <div class="sidebar-section">
          <h3>Recent Posts</h3>
          <ul class="recent-posts">
            {% for post in collections.recentPosts %}
              <li>
                <a href="{{ post.url }}">{{ post.data.title }}</a>
                <span class="post-date">{{ post.date | blogDate }}</span>
              </li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
</section> 